import React from 'react';
import classNames from 'classnames';

function EmptyTimeCard() {
  return <div className={classNames('time-select-time-card', 'no-time')} />;
}

export default EmptyTimeCard;
